ADMIN_USERNAME=Alqabbani
ADMIN_PASSWORD=your-password-here
JWT_SECRET=change-this-to-a-random-secret

# Option 1 — Vercel Blob (recommended): Dashboard → Storage → Create Blob → Connect to project
# BLOB_STORE_ID and VERCEL_OIDC_TOKEN are added automatically
# BLOB_READ_WRITE_TOKEN=

# Option 2 — Upstash Redis: Dashboard → Storage → Redis → Connect to project
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
